var n = require('./dist/nuclear')
var s = require('./dist/nuclear').Store
var r = new n.Reactor({
  debug:true,
})
r.registerStores({
  test: s({
    getInitialState: function () {
      return 'foo'
    }
  })
})
r.dispatch('hey')
console.assert(r.evaluateToJS([]).test === 'foo')
